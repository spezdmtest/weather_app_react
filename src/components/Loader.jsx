import React from "react";
import LoaderImage from "/img/loader.gif";

const Loader = () => {
  return 
    <img
      className="loader"
      src={LoaderImage}
      alt="loader"
    />
};

export default Loader;
